# hotkey_trigger

## Setup
Install requirements: `pip install -r requirements.txt`

## Usage
Alt + Z will bring up a dialog where any command typed will either run a predefined command or will simply input the command into Windows Terminal if it's not a predefined command.

For example, `vagrantup` will go to the specified directory and run `vagrant up` commmand.
